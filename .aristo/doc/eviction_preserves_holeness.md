**Aristo verified intent — `eviction_preserves_holeness`**

An accepted eviction returns the evicted key's downstream materialization to a re-fillable hole — never a filled-but-stale region, and never a known-empty result.

<sub>Verify level: **neural**</sub>

---
